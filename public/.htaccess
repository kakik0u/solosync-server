RewriteEngine On

<IfModule mod_headers.c>
  <FilesMatch "^admin\.(?:js|css)$">
    Header set Cache-Control "no-store, max-age=0"
  </FilesMatch>
</IfModule>

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?route=/$1 [QSA,L]
